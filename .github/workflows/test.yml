name: Autotest

on:
  push

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: set up java
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: '17'

      - name: Install Chrome
        run: sudo apt-get install google-chrome-stable

      - name: Run tests
        run: ./gradlew clean test

      - name: Save allure results
        uses: actions/upload-artifact@v3
        with:
          name: allure-results
          path:
            allure-results

  generate-report:
    if: always()
    runs-on: ubuntu-latest
    needs: test
    name: Generate Report
    steps:
      - uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: '17'
      - run: sudo wget https://github.com/allure-framework/allure2/releases/download/2.37.0/allure-2.37.0.tgz && sudo tar -zxvf allure-2.37.0.tgz -C /opt/ && sudo ln -s /opt/allure-2.37.0/bin/allure /usr/bin/allure
      - name: Download all workflow artifact
        uses: actions/download-artifact@v4
      - run: allure generate -c allure-results -o -site
      - name: Store generated report
        uses: actions/upload-artifact@v4
        with:
          name: _site
          path: _site


  publish-report:
    if: always()
    runs-on: ubuntu-latest
    needs: generate-report
    name: Report publication
    steps:
      - name: Download all workflow artifact
        uses: actions/download-artifact@v4
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v1.2.9

          

#      - name: Install Allure
#        if: always()
#        run: |
#          sudo apt-get install -y wget unzip
#          wget https://repo.maven.apache.org/maven2/io/qameta/allure/allure-commandline/2.37.0/allure-commandline-2.37.0.zip
#          unzip allure-commandline-2.37.0.zip
#          sudo mv allure-2.37.0 /opt/allure
#          sudo ln -s /opt/allure/bin/allure /usr/bin/allure
#
#      - name: Generate allure report
#        if: always()
#        run: allure generate build/allure-results --clean -o build/allure-report
#
#      - name: Check allure report
#        if: always()
#        run: ls -l build/allure-report
#
#      - name: Deploy Allure Report
#        if: always()
#        uses: peaceiris/actions-gh-pages@v3
#        with:
#            github_token: ${{ secrets.GH_PAT }}
#            publish_dir: build/allure-report
#
#      - name: Upload Allure Report
#        if: always()
#        uses: actions/upload-artifact@v4
#        with:
#          name: allure-report
#          path: build/allure-report


